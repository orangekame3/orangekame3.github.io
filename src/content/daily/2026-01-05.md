---
date: 2026-01-05
---

::ja

今日はmirrormateにVision Language Model(VLM)を実装した。VLMについては初めてしったが、Ollamaで使えそうなモデルを適当に選択して試している。応答がやや遅いが`llava:7b`を一旦試している。
chatについては日本語特化モデルをいくつか試したが結局`gpt-oss:20b`が安定して良いパフォーマンスなのでこれを採用している。

::en

<!-- draft:

Today, I implemented Vision Language Model into mirrormate. I didn't know that VLM. I tried some model available on an Ollama. Responsibility is not so fast, but I use `llava:7b` on trial. As a chat, I tried some japanese specific model, but in eventually, `gpt-oss:20b` is stable and good performance, so I use this model.

-->

<!-- review:

- "I didn't know that VLM" → "I didn't know about VLM until now" (「知らなかった」という過去の状態を表すには "didn't know about" が自然。"that VLM" は文法的に不完全)
- "I tried some model available on an Ollama" → "I tried some models available on Ollama" (model は複数形に、Ollama は固有名詞なので冠詞不要)
- "Responsibility is not so fast" → "The response is a bit slow" (「応答」は "response"。"Responsibility" は「責任」の意味になってしまう)
- "I use `llava:7b` on trial" → "I'm trying `llava:7b` for now" (「一旦試している」のニュアンスは "trying ... for now" が自然)
- "As a chat" → "For chat" (用途を示す前置詞は "for" が適切)
- "but in eventually" → "but eventually" (in は不要)
- "is stable and good performance" → "has stable and good performance" (名詞 performance には動詞 "has" を使う)

-->

Today, I implemented a Vision Language Model (VLM) into MirrorMate. I didn't know about VLM until now, but I tried some models available on Ollama. The response is a bit slow, but I'm trying `llava:7b` for now. For chat, I tried some Japanese-specific models, but eventually, `gpt-oss:20b` has stable and good performance, so I'm using this model.
